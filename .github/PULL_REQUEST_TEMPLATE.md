## What does this change?

<!-- One or two sentences. Link the issue if there is one. -->

## Checklist

- [ ] `uvx ruff check src tests` and `uvx ruff format --check src tests` pass
- [ ] `pytest -m "not integration"` passes
- [ ] Ran `pytest -m integration` (only needed if you touched pipeline/recorder/detector)
- [ ] Docs updated if behaviour changed
